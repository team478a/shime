# SHIME AI引き継ぎ状況

- 更新日: 2026-07-25
- 本番目標: 2026-08-08
- 対象リポジトリ: `team478a/shime`
- 作業ブランチ: `release/2026-08-08-readiness`
- ベース: `main` / `b07d1ce3bfe4a3ba878cc9490002802201ffa79b`
- 現在の判定: **Go / No-Go未判定。本番可能とは判定しない**

## 1. この文書の役割

CodexとClaude Codeの会話履歴は共有されない。
この文書、Git履歴、既存の本番準備資料を、交代時の共通状態として使用する。

作業開始時と終了時に必ず更新すること。

## 2. 現在確認できている状態

### 実装・検証済みの主要項目

- Next.js / React / TypeScript / Drizzle / PostgreSQL / Supabaseを基盤としている。
- lint、format、architecture check、typecheck、unit、integration、build、E2E、readiness用スクリプトがある。
- staging環境、Supabase migration、private Storage、バックアップ基盤が確認されている。
- 管理者認証、権限、イベント境界、未認証API拒否が確認されている。
- CSV取込後の参加者生成不具合は修正・再試験済み。
- イベント単位の決定論的な参加者番号採番は修正・再試験済み。
- 合成参加者RH-A01で、LINE本人連携、Dream登録、5問、PASS、QR再発行、手動受付、理由付き取消、再受付まで確認済み。
- 手動受付、本人連携一覧、管理ナビゲーション、席調整画面のスマートフォン改善が実装済み。
- 会場テンプレートの版管理、イベント適用、スナップショット、アーカイブ保持はリハーサル済み。
- 過去に記録されたP0不具合2件と会場テンプレートP1不具合1件は再試験合格済み。

### 未完了または未判定の主要項目

- `EVENT_CONFIG_20260808.yaml` に `REQUIRED_INPUT` が残っている。
- 本番用の正式イベント値、正式規約、保存期間などが確定していない。
- LINE本人連携は一部実機確認済みだが、期限切れ・再利用拒否を含む全シナリオは未完了。
- QRカメラ受付の実機確認が未完了。
- Dream非公開、Dream任意スキップ、質問途中保存・回答辞退の実機確認が未完了。
- 欠席後の席再計算、manager公開、公開前非表示の通し確認が未完了。
- 希望入力3方式、一方希望非公開、複数成立競合、manager確定が未完了。
- 結果通知のプレビュー、送信、失敗、再送、二重送信防止の実サービス確認が未完了。
- 通常CSV、責任者限定CSV、紙の受付表・席表による代替運用確認が未完了。
- 高頻度ジョブ実行基盤、監視・通知、production昇格、独自ドメインの最終確認が未完了。
- 匿名化50名・受付端末5台による本番相当リハーサルが未完了。
- Go / No-Go判定は未実施。

## 3. 現在のP0作業順

### P0-A: 現在状態の再同期

目的:

- 最新コード、最新コミット、既存資料の差分を確認する。
- 古くなった本番準備記録を、実際の検証結果と一致させる。
- 未実施、実装不足、外部設定待ちを区別する。

完了条件:

- `PHASE8_READINESS_REPORT.md` と `REHEARSAL_EXECUTION_RECORD_20260715.md` の状態が最新コードと矛盾しない。
- 各未完了項目に「実装」「自動試験」「実機試験」「外部入力」の分類がある。
- 次に実装すべきP0が1件に絞られている。

状態: `next`

### P0-B: 非ブロック項目の全導線リハーサル

外部の正式イベント情報が未確定でも、合成イベントRH-A/B/Cで次を完了する。

1. 利用済み・期限切れ本人連携URLの拒否
2. Dream非公開と任意スキップ
3. 5問の途中保存・回答辞退
4. QRカメラ受付、二重受付警告
5. 欠席後の席再計算、手動調整、manager公開
6. 希望入力3方式、0名提出、締切後拒否
7. 一方希望、順位、私的メモの非公開
8. 複数成立競合、manager確定、system_admin取消
9. 結果通知プレビュー、送信、失敗再送
10. 通常CSV・責任者限定CSV・紙代替運用

状態: `not_started`

### P0-C: ジョブ・監視・復旧

- 通知ジョブ5分、監視ジョブ10分相当の実行方法を確定する。
- 同時実行、重複実行、失敗再試行を確認する。
- バックアップ取得と復旧手順を本番担当者が実行できる形にする。
- LINEまたはAI停止時の代替運用を通し確認する。

状態: `not_started`

### P0-D: 正式イベント設定

主催者・運営責任者から次を確定し、管理画面と設定ファイルへ反映する。

- 正式イベント名
- 終了日時
- 会場名・住所
- 申込開始・終了日時
- 希望入力開始・締切日時
- 参加区分A/Bの正式名称
- 席替え回数
- 感情カードセット
- 保存期間
- イベント規約版
- プライバシーポリシー版

注意:

- AIが推測で決定しない。
- 値が未確定の間は受付開始・本番判定を行わない。

状態: `external_input_required`

### P0-E: production昇格と最終Go判定

- production環境変数と外部接続を確認する。
- migration、Storage、ジョブ、監視、独自ドメインを確認する。
- 匿名化50名、受付端末5台で本番相当リハーサルを行う。
- P0が0件であることをmanagerとsystem_adminが確認する。
- 明示的な承認後にのみproductionへ昇格する。

状態: `blocked_by_P0_B_C_D`

## 4. 次のAIへの具体的指示

次の作業は **P0-A: 現在状態の再同期** とする。

```text
SHIMEの2026年8月8日本番準備を継続してください。

最初にAGENTS.md、CLAUDE.md、docs/shime/AI_HANDOFF.md、
docs/shime/PHASE8_READINESS_REPORT.md、
docs/shime/REHEARSAL_EXECUTION_RECORD_20260715.md、
docs/shime/PHASE8_REHEARSAL_CHECKLIST.mdを確認してください。

git status、現在ブランチ、直近10コミットを確認し、
最新実装と本番準備資料の差分を調査してください。

今回は新機能を追加せず、P0-Aだけを行ってください。
未完了項目を次の4分類に分けてください。

- 実装不足
- 自動試験不足
- 実機・運用試験不足
- 主催者または外部設定待ち

調査結果を本番準備資料とAI_HANDOFF.mdへ反映してください。
その後、外部入力なしで着手できる次のP0を1件だけ提案してください。
```

## 5. 交代前に必ず記録する項目

- 実行AI: Codex / Claude Code
- 作業開始コミット
- 作業終了コミット
- 完了したP0項目
- 変更ファイル
- 実行した検証コマンド
- 成功・失敗・未実行
- 新規に発見したP0/P1/P2
- 外部確認が必要な事項
- 次に実行する1件

## 6. 最終更新記録

| 日時 | 実行者 | 開始コミット | 終了コミット | 内容 | 次の作業 |
|---|---|---|---|---|---|
| 2026-07-25 | ChatGPT | `b07d1ce` | 文書作成コミット | Codex／Claude Code交代運用の初期状態を作成 | P0-A 現在状態の再同期 |
