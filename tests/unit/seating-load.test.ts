import { describe, expect, it } from "vitest"; import { buildDeterministicAssignments } from "@shime/core";
describe("seating load baseline", () => { it("places 200 participants deterministically", () => { const candidates = Array.from({ length: 200 }, (_, index) => ({ id: `p${String(index).padStart(3, "0")}`, category: index % 2 ? "B" : "A", checkedIn: true, answerKey: String(index) })); const seats = Array.from({ length: 200 }, (_, index) => ({ id: `s${String(index).padStart(3, "0")}`, tableId: `t${String(Math.floor(index / 2)).padStart(3, "0")}`, order: index % 2 })); const pairs = Array.from({ length: 100 }, (_, index) => ({ a: candidates[index * 2]!.id, b: candidates[index * 2 + 1]!.id, score: 100 - index / 100, sharedTopicCount: 1 })); const input = { candidates, seats, pairs, allowedCategoryPairs: [["A", "B"]], blockedPairs: [] }; const first = buildDeterministicAssignments(input); const second = buildDeterministicAssignments(input); expect(first).toEqual(second); expect(first.assignments).toHaveLength(200); expect(first.unassignedParticipantIds).toEqual([]); }, 2_000); });
