import { and, eq } from "drizzle-orm";
import { NextResponse } from "next/server";
import { z } from "zod";
import { emotionCards, emotionSelections, getDatabase, participants } from "@shime/db";
import { requireParticipantForEvent } from "@shime/web/server/participant-auth";
const input = z.object({ emotionCardId: z.string().uuid(), firstImpression: z.string().trim().min(1).max(500), relatedArea: z.string().trim().min(1).max(500), underlyingWish: z.string().trim().min(1).max(500), freeText: z.string().trim().max(2000).optional(), redraw: z.boolean().default(false), finalize: z.boolean().default(false) });
export async function POST(request: Request, { params }: { params: Promise<{ eventId: string }> }) { const { eventId } = await params; const auth = await requireParticipantForEvent(eventId).catch(() => null); if (!auth) return NextResponse.json({ code: "UNAUTHORIZED" }, { status: 401 }); const parsed = input.safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ code: "INVALID_INPUT" }, { status: 400 }); const db = getDatabase(); const card = await db.select({ id: emotionCards.id }).from(emotionCards).where(and(eq(emotionCards.id, parsed.data.emotionCardId), eq(emotionCards.tenantId, auth.session.tenantId), eq(emotionCards.active, true))).limit(1); if (!card[0]) return NextResponse.json({ code: "CARD_NOT_FOUND" }, { status: 404 }); const existing = await db.select().from(emotionSelections).where(and(eq(emotionSelections.tenantId, auth.session.tenantId), eq(emotionSelections.eventId, eventId), eq(emotionSelections.participantId, auth.participant.id))).limit(1); if (existing[0]?.finalizedAt) return NextResponse.json({ code: "SELECTION_FINALIZED" }, { status: 409 }); const redrawCount = (existing[0]?.redrawCount ?? 0) + (parsed.data.redraw ? 1 : 0); if (redrawCount > 1) return NextResponse.json({ code: "REDRAW_LIMIT_EXCEEDED" }, { status: 409 }); const values = { emotionCardId: parsed.data.emotionCardId, firstImpression: parsed.data.firstImpression, relatedArea: parsed.data.relatedArea, underlyingWish: parsed.data.underlyingWish, freeText: parsed.data.freeText, redrawCount, finalizedAt: parsed.data.finalize ? new Date() : null, updatedAt: new Date() }; const [saved] = existing[0] ? await db.update(emotionSelections).set(values).where(eq(emotionSelections.id, existing[0].id)).returning() : await db.insert(emotionSelections).values({ tenantId: auth.session.tenantId, eventId, participantId: auth.participant.id, ...values }).returning(); await db.update(participants).set({ dreamState: "drafting", updatedAt: new Date() }).where(eq(participants.id, auth.participant.id)); return NextResponse.json({ data: saved }); }
