import { and, eq } from "drizzle-orm"; import { NextResponse } from "next/server"; import { requirePermission, findMatchConflicts } from "@shime/core"; import { applications, events, getDatabase, matchCandidates, participants, preferenceSubmissions } from "@shime/db"; import { requireStaffSession } from "@shime/web/server/auth";
export async function GET(_request: Request, { params }: { params: Promise<{ eventId: string }> }) { const { eventId } = await params; const session = await requireStaffSession().catch(() => null); if (!session) return NextResponse.json({ code: "UNAUTHORIZED" }, { status: 401 }); try { requirePermission(session.role, "preference:read"); } catch { return NextResponse.json({ code: "FORBIDDEN" }, { status: 403 }); } const db = getDatabase(); const event = await db.select().from(events).where(and(eq(events.tenantId, session.tenantId), eq(events.id, eventId))).limit(1); if (!event[0]) return NextResponse.json({ code: "NOT_FOUND" }, { status: 404 }); const candidates = await db.select().from(matchCandidates).where(and(eq(matchCandidates.tenantId, session.tenantId), eq(matchCandidates.eventId, eventId))); const people = await db.select({ id: participants.id, participantNumber: participants.participantNumber, fullName: applications.fullName }).from(participants).innerJoin(applications, and(eq(applications.id, participants.applicationId), eq(applications.tenantId, participants.tenantId))).where(and(eq(participants.tenantId, session.tenantId), eq(participants.eventId, eventId))); const submissions = await db.select().from(preferenceSubmissions).where(and(eq(preferenceSubmissions.tenantId, session.tenantId), eq(preferenceSubmissions.eventId, eventId))); return NextResponse.json({ data: { eventName: event[0].name, eventStatus: event[0].status, preferenceMode: event[0].preferenceMode, allowMultipleMatches: event[0].allowMultipleMatches, candidates, participants: people, submissionSummary: { submitted: submissions.filter((s) => s.status === "submitted").length, total: people.length }, conflicts: findMatchConflicts(candidates, event[0].allowMultipleMatches) } }); }
